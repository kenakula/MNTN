mixin card(item)
  li.card&attributes(attributes)
    .card__info
      span.card__index !{item.index}
      h3.card__subtitle !{item.subtitle}
      p.card__title !{item.title}
      p.card__text !{item.text}
    .card__img
      +img(item.image)
